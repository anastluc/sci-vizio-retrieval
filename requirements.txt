# Core pipeline
PyMuPDF>=1.23.0
Pillow>=10.0.0
numpy>=1.24.0
chromadb>=0.4.22
open-clip-torch

# OpenRouter (uses OpenAI SDK with custom base_url)
openai
requests>=2.31.0

# ML / embeddings
torch>=2.0.0
torchvision

# Utilities
python-dotenv
tqdm

# UI
gradio